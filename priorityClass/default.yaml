apiVersion: scheduling.k8s.io/v1
kind: PriorityClass
metadata:
  name: default
value: 1000000
preemptionPolicy: Never
globalDefault: true
description: "Default priority. This priority class will not cause other pods to be preempted."
